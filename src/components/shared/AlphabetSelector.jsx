// Reusable dropdown for creating, renaming, deleting, and switching alphabets

/** AlphabetSelector.jsx — stub */
export default function AlphabetSelector() {
  return <div>AlphabetSelector — stub</div>;
}