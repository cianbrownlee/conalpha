// Renders a single glyph image or falls back to raw IPA text if no drawing exists

/** GlyphRenderer.jsx — stub */
export default function GlyphRenderer() {
  return <div>GlyphRenderer — stub</div>;
}