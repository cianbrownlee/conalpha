// Dropdown to pick a reference language profile (English, Spanish, Arabic, etc.)

/** LanguageSelector.jsx — stub */
export default function LanguageSelector() {
  return <div>LanguageSelector — stub</div>;
}